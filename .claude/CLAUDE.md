For GitHub URLs, use `gh` CLI commands (e.g., `gh pr view <url>`) instead of WebFetch for authenticated access to private repos.

Look for `pnpm-lock.yaml` (pnpm), `yarn.lock` (yarn), or `package-lock.json` (npm) to determine which package manager to use for installs and scripts.
